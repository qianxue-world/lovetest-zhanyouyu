apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: mbti-personality-test
  namespace: mbti-test

resources:
- namespace.yaml
- deployment.yaml
- service.yaml
- ingress.yaml
- configmap.yaml
- env-configmap.yaml
- hpa.yaml

images:
- name: omaticaya/lovetest-mbti
  newTag: latest

commonLabels:
  app: lovetest-mbti
  managed-by: argocd


namespace: mbti-test