apiVersion: v1
kind: Service
metadata:
  name: lovetest-zhanyouyu-service
  namespace: lovetest-zhanyouyu-ns
  labels:
    app: lovetest-zhanyouyu
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: lovetest-zhanyouyu
